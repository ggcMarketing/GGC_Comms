<UserControl x:Class="IndustrialGateway.UI.Views.S7ConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IndustrialGateway.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="10">
            <!-- Connection Settings -->
            <GroupBox Header="Connection Settings">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Label Grid.Row="0" Grid.Column="0" Content="Host/IP:"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Host, UpdateSourceTrigger=PropertyChanged}"/>

                    <Label Grid.Row="1" Grid.Column="0" Content="Port:"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Port, UpdateSourceTrigger=PropertyChanged}"/>

                    <Label Grid.Row="2" Grid.Column="0" Content="CPU Type:"/>
                    <ComboBox Grid.Row="2" Grid.Column="1" 
                             ItemsSource="{Binding AvailableCpuTypes}"
                             SelectedItem="{Binding CpuType}"/>

                    <Label Grid.Row="3" Grid.Column="0" Content="Rack:"/>
                    <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Rack, UpdateSourceTrigger=PropertyChanged}"/>

                    <Label Grid.Row="4" Grid.Column="0" Content="Slot:"/>
                    <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Slot, UpdateSourceTrigger=PropertyChanged}"/>

                    <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Connect" Command="{Binding ConnectCommand}"/>
                        <Button Content="Disconnect" Command="{Binding DisconnectCommand}"/>
                    </StackPanel>
                </Grid>
            </GroupBox>

            <!-- Status -->
            <GroupBox Header="Status">
                <StackPanel>
                    <TextBlock Text="{Binding StatusMessage}" FontWeight="Bold"/>
                </StackPanel>
            </GroupBox>

            <!-- Tags Configuration -->
            <GroupBox Header="Tags">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Add Tag" Command="{Binding AddTagCommand}"/>
                    </StackPanel>

                    <DataGrid ItemsSource="{Binding Tags}" 
                             AutoGenerateColumns="False"
                             CanUserAddRows="False"
                             SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="150"/>
                            <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="150">
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader">
                                        <Setter Property="ToolTip" Value="e.g., DB1.DBW0, DB1.DBD10, M0.0"/>
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Data Type" Binding="{Binding DataType}" Width="100"/>
                            <DataGridTextColumn Header="Value" Binding="{Binding TestValue}" Width="100" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="200" IsReadOnly="True"/>
                            <DataGridTemplateColumn Header="Actions" Width="150">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Read" 
                                                   Command="{Binding DataContext.ReadTagCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                   CommandParameter="{Binding}"
                                                   Margin="2"/>
                                            <Button Content="Remove" 
                                                   Command="{Binding DataContext.RemoveTagCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                   CommandParameter="{Binding}"
                                                   Margin="2"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </GroupBox>

            <!-- Help Text -->
            <GroupBox Header="S7 Address Format">
                <TextBlock TextWrapping="Wrap">
                    <Run Text="Examples of valid S7 addresses:"/>
                    <LineBreak/>
                    <Run Text="• DB1.DBW0 - Data Block 1, Word at byte 0"/>
                    <LineBreak/>
                    <Run Text="• DB1.DBD10 - Data Block 1, Double Word at byte 10"/>
                    <LineBreak/>
                    <Run Text="• DB1.DBB5 - Data Block 1, Byte at byte 5"/>
                    <LineBreak/>
                    <Run Text="• M0.0 - Memory bit 0.0"/>
                    <LineBreak/>
                    <Run Text="• I0.0 - Input bit 0.0"/>
                    <LineBreak/>
                    <Run Text="• Q0.0 - Output bit 0.0"/>
                </TextBlock>
            </GroupBox>
        </StackPanel>
    </ScrollViewer>
</UserControl>
